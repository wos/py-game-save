Game state saving service
-------------------------

Currently only for Monopoly game.


  ## Installlation:

  ### For development issue:
  ```
    pip install -r requirements.txt
    npm install
  ```
 
  ### Build JavaScript 
  ```
    npm run watch
  ```
  
  ### Run python backend:    
  ```
    python index.py
  ```
